# Objective 5 pre-test model-selection and calibration lock

Both target-adapted DenseNet-121 candidates passed the locked validation advancement rule. One scalar temperature and six decision thresholds per dataset were fitted only on target validation data and frozen before locked-test access. No patient or image identifiers, medical images, private manifests, or case-level predictions are included.
